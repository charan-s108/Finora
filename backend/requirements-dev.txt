# =============================================================================
# FINORA BACKEND — Development / Evaluation Dependencies
# Install AFTER requirements.txt: pip install -r requirements-dev.txt
# These are NOT installed in Railway (production).
# =============================================================================

-r requirements.txt

# MCP server (standalone process — separate from FastAPI)
# NOTE: fastmcp requires mcp SDK which pulls starlette>=0.49, incompatible
# with fastapi 0.115. Install in a SEPARATE venv for MCP server only.
# fastmcp==1.0

# Evaluation
ragas==0.2.5
datasets==3.1.0

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0

# Linting / formatting
ruff==0.7.1
black==24.10.0
mypy==1.13.0
