# =============================================================================
# FINORA BACKEND — Production Dependencies
# For Railway deployment. Dev/eval/MCP tools are in requirements-dev.txt.
# =============================================================================

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
httpx>=0.27.0,<0.28.0

# Validation
pydantic==2.9.2
pydantic-settings==2.5.2

# Rate limiting
slowapi==0.1.9

# LLM — Groq
groq==0.11.0

# Agent graph
langgraph==0.2.55
langgraph-checkpoint==2.0.6

# RAG / LangChain
langchain==0.3.7
langchain-community==0.3.7
langchain-groq==0.2.1
langchain-qdrant==0.2.0
langsmith==0.1.147

# Vector store
qdrant-client==1.11.3

# Reranker — Cohere primary, BAAI/bge-reranker-base local fallback
cohere==5.11.4

# Embeddings + local reranker fallback (HuggingFace)
sentence-transformers==3.2.1
transformers==4.46.3
torch==2.4.1

# Sparse retrieval
rank-bm25==0.2.2

# Fuzzy search
rapidfuzz==3.10.1

# Financial data — all fetching via curl_cffi Chrome TLS impersonation (no API key needed)
curl_cffi==0.15.0
polygon-api-client==1.14.2
pandas==2.2.3
lxml==5.3.0
html5lib==1.1

# News
newsapi-python==0.2.7

# SEC filings
unstructured==0.15.14
requests==2.32.3

# Scheduling
apscheduler==3.10.4

# Logging
structlog==24.4.0
python-dotenv==1.0.1
openai>=1.0.0
